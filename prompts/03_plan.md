# Фаза 3: Implementation Plan

Прочитай этот файл и выполни всё что написано ниже.

---

## Задачи

1. Прочитай `docs/PRD.md`
2. Используй `superpowers:writing-plans` skill — создай `docs/PLAN.md` по vertical slice подходу: каждый слайс рабочий от данных до результата, задачи по 2-4 часа, для каждой: файл → что делает → input → output
3. Создай структуру папок: `data/raw`, `data/processed`, `src/data`, `src/nlp`, `src/env`, `src/agents`, `src/eval`, `notebooks`, `tests`, `experiments`, `results`, `configs`, `docs`
4. Создай базовые файлы: `requirements.txt`, `.gitignore`, `README.md`, `__init__.py` во всех `src/*` папках

## Завершение

Обнови CLAUDE.md (текущий фокус → Фаза 4, задача 1) и WORKFLOW.md (✅ Фаза 3).
